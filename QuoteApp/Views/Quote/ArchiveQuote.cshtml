@model QuoteApp.Models.Quote
@{
    ViewBag.Title = "ArchiveQuote";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>Are you sure you want to archive this?</h3>
<div>
    <h4>Quote</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.QuoteId)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.QuoteId)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.QuoteDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.QuoteDate)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Archive" class="btn btn-default" /> |
            @Html.HiddenFor(m=> m.QuoteId);
            @Html.ActionLink("No, sorry", "Index", "Home")
        </div>
    }
</div>
